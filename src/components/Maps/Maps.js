import React from 'react'
import PropTypes from 'prop-types'
import classes from './Maps.module.scss'

export const Maps = () => <div className={classes.Maps}></div>
Maps.propTypes = {}
export default Maps
